# Netlify Configuration

[build]
  # Build command
  command = "pnpm install && pnpm build"
  
  # Publish directory
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
